import 'package:flutter/material.dart';

class SearchedCustomerItem extends StatelessWidget {
  const SearchedCustomerItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
